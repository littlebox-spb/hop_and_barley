# 🛒 Internet Shop

Интернет-магазин на Django с REST API, JWT-аутентификацией и Docker-окружением.  Проект разрабатывается как full-stack backend с упором на чистую архитектуру и удобство деплоя.
Проект реализует классический e-commerce сценарий: каталог товаров, корзина, оформление заказов, уведомления по email и API для дальнейшего расширения.

---

## 🚀 Технологический стек

- **Python 3.13**
- **Django**
- **Django REST Framework**
- **Simple JWT**
- **PostgreSQL**
- **Poetry**
- **Docker / Docker Compose**
- **SMTP (email-уведомления)**

---

## 📦 Основные возможности

- REST API для интернет-магазина
- JWT-аутентификация (access / refresh)
- Работа с PostgreSQL
- Сборка и запуск через Docker
- Управление зависимостями через Poetry
- Статические файлы через `collectstatic`

## 🚀 Функциональность

### 👤 Пользовательская часть

- Каталог товаров
  - поиск
  - сортировка (price / name / popularity)
  - пагинация
- Карточка товара
  - описание
  - рейтинг и отзывы
  - добавление в корзину
  - отзыв о товаре с рейтингом
- Корзина
  - изменение количества товаров
  - удаление товаров
  - пересчет общей суммы
- Оформление заказа
  - проверка остатка товара (`stock`)
  - создание заказа и позиций заказа
  - уведомление по email
- Авторизация / регистрация пользователей
- История заказов в профиле
- Панель администратора
- добавление товара
- редактирование товара
- загрузка изображения товара

---

## 📁 Структура проекта (упрощенно)

hop_and_barley/  
├── admin_panel/  
│ ├── views.py  
│ └── urls.py  
├── products/  
│ ├── models.py  
│ └── views.py  
├── orders/  
│ ├── models.py  
│ ├── views.py  
│ └── cart.py  
├── reviews/  
│ ├── models.py  
│ └── views.py  
├── users/  
│ └── views.py
├── static/  
│ ├── css/  
│ ├── js/  
│ └── img/products/  
├── templates/  
│ └── base.html  
└── manage.py

---

## ⚙️ Переменные окружения

Проект использует `.env` файл.

Пример `.env`:

```
SECRET_KEY=django-secret-key

POSTGRES_DB=shop_db
POSTGRES_USER=shop_user
POSTGRES_PASSWORD=shop_password
POSTGRES_HOST=db
POSTGRES_PORT=5432

EMAIL_HOST=your smtp server
EMAIL_PORT=port smtp server
EMAIL_USE_SSL=True
EMAIL_USE_TLS=False

EMAIL_HOST_USER="login"
EMAIL_HOST_PASSWORD="password"

DEFAULT_FROM_EMAIL=email shop (From)

DEBUG=True
```

## 🐳 Запуск проекта через Docker

### 1. Собрать и запустить контейнеры

`docker compose up --build`

### 2. Создать суперпользователя

`docker compose exec web python manage.py createsuperuser`

#### Проект будет доступен по адресу: <http://localhost:8000>

## 🧪 Запуск тестов

### Тесты запускаются **внутри контейнера**

`docker compose exec web python manage.py test`

### Фейковые данные для тестов

`docker compose exec web python seed.py`

## 🔐 API

В проекте используется **JWT (Simple JWT)**.
Основные эндпоинты:

- `POST /api/token/` — получение access / refresh токенов
- `POST /api/token/refresh/` — обновление access токена

- `GET /api/products/` — получить список продуктов
- `GET /api/products/` — получить список заказов

- OpenAPI-документация (drf-spectacular)

---

## ⚠️ Ограничения

- Проект учебный
- Не используется `MEDIA_ROOT`
- Нет системы платежей (статус заказа условный)
- Минимальная валидация на фронте

---

## 📄 Лицензия

Проект распространяется под лицензией MIT.
